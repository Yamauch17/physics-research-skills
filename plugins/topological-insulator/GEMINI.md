# topological-insulator (Gemini context)

Auto-activating Agent Skills live in `./skills/` and activate when a task matches a skill's
`description`. This is a **domain pack** — install the `physics-research` core too. Tool-name mapping
for Gemini CLI: see the repo README.

@./README.md
